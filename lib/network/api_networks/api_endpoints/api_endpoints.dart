/// Path segments and route templates; keep them centralized to avoid typos.
abstract final class ApiEndpoints {
  static const String health = '/health';
  // Example: static const String login = '/auth/login';
}
